<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="cron_customer_statement" model="ir.cron">
            <field name="name">Send Customer Statements</field>
            <field name="model_id" ref="base.model_res_partner" />
            <field name="state">code</field>
            <field name="code">model.send_customer_statement()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <!-- <field name="nextcall"
                eval="(DateTime.now().replace(day=1) + timedelta(month=1)).strftime('%Y-%m-%d %H:%M:%S')" /> -->
            <field name="numbercall">-1</field>
        </record>
    </data>
</odoo>