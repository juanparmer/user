<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="mail_customer_statement" model="mail.template">
            <field name="name">Customer Statements</field>
            <field name="model_id" ref="base.model_res_partner" />
            <field name="email_from">{{ object._get_followup_responsible().email_formatted }}</field>
            <field name="partner_to">{{ object.id }}</field>
            <field name="subject">{{ (object.company_id or object._get_followup_responsible().company_id).name }} Customer Statement - {{ object.commercial_company_name }}</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px;">
                        <t t-if="object.id != object.commercial_partner_id.id">
                            Dear <t t-out="object.name or ''" /> (<t t-out="object.commercial_partner_id.name or ''" />),
                        </t>
                        <t t-else="">
                            Dear <t t-out="object.name or ''" />,
                        </t>
                        <br /> 
                        It has come to our attention that you have an outstanding balance of <t t-out="format_amount(object.total_overdue, object.currency_id) or ''" />
                        <br />
                        We kindly request that you take necessary action to settle this amount
                        within the next 8 days. <br /> If you have already made the payment after
                        receiving this message, please disregard it. Our accounting department is
                        available if you require any assistance or have any questions. <br /> Thank
                        you for your cooperation. <br /> Sincerely, 
                        <t t-if="not is_html_empty(object._get_followup_responsible().signature)">
                            <br />
                            <t t-out="object._get_followup_responsible().signature" />
                        </t>
                        <t t-else="">
                            <br /> -- <br />
                            <t t-out="object._get_followup_responsible().name" />
                        </t>
                    </p>
                </div>
            </field>
            <field name="lang">{{ object.lang }}</field>
            <field name="auto_delete" eval="False" />
            <field name="report_template_ids" eval="[(4, ref('statement_user.report_customer_statement'))]" />
        </record>
    </data>
</odoo>