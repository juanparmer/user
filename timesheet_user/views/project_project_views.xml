<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="edit_project_project" model="ir.ui.view">
        <field name="name">project.project.view.form.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='group_time_managment']" position="inside">
                <group>
                    <!-- <field name="timesheet_email_from" placeholder="Override author's email"/> -->
                    <!-- <field name="timesheet_email_to" invisible="not allow_timesheets" placeholder="Comma-separated recipient addresses" /> -->
                    <!-- <field name="timesheet_email_cc" invisible="not allow_timesheets" placeholder="Comma-separated carbon copy recipients addresses" /> -->
                    <field name="timesheet_report" invisible="not allow_timesheets" />
                </group>
            </xpath>
        </field>
    </record>
</odoo>